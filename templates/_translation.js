const elements = [
  Element('header') //replace for your id
    .caixa({
      width: '100px',
      height: '0px',
      top: '-500px',
      left: '50px',
      // style atributes
    }),
]
createElements(elements)

timeLine([])
